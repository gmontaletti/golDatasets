% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gol_extract.R
\name{gol_extract_series}
\alias{gol_extract_series}
\title{Estrai una serie temporale pronta per plot da \code{gol_inapp_mensile}}
\usage{
gol_extract_series(
  data = NULL,
  variabile,
  etichetta = NULL,
  tavola = NULL,
  percorso = NULL,
  unita_misura = "valore_assoluto"
)
}
\arguments{
\item{data}{Un \code{data.table} con la stessa struttura di
\code{gol_inapp_mensile}. Se \code{NULL} (default), usa il dataset esposto.}

\item{variabile}{Nome della variabile (es. \code{"occupati_totale"},
\code{"raggiunti"}, \code{"individui"}, \code{"con_politica"}).}

\item{etichetta}{Vettore di etichette regionali. \code{NULL} (default) per
tutte; passare ad esempio \code{"Italia"} o \code{"Totale"} per l'aggregato
nazionale, o un vettore di regioni per il confronto.}

\item{tavola}{Numero di tavola (1.1, 1.2, 2.1, 2.2). \code{NULL} per
inferenza automatica dalla \code{variabile}.}

\item{percorso}{Filtro sulla colonna \code{percorso}. Default \code{NULL}
(nessun filtro: le variabili che esistono per un solo percorso
restituiscono una serie unica, quelle che si decompongono mantengono
la colonna \code{percorso}). Passare \code{""} per restringere all'aggregato
senza disaggregazione, o un singolo percorso
(\code{"1_reinserimento_lavorativo"}, ...) per filtrare.}

\item{unita_misura}{Default \code{"valore_assoluto"}. Altri valori possibili:
\code{"percentuale"}, \code{"percentuale_riga"}.}
}
\value{
Un \code{data.table} con almeno le colonne \code{data} (IDate) e \code{valore}
(numeric). Se \code{etichetta} ha piu' valori, e' presente anche \code{regione}.
Se \code{percorso = NULL}, e' presente anche \code{percorso}.
}
\description{
Filtra \code{gol_inapp_mensile} lungo tavola, variabile, percorso, etichetta
regionale e unita' di misura, restituendo un \code{data.table} con le colonne
\code{data}, \code{valore} (ed eventualmente \code{regione}, \code{percorso}) pronto per
essere passato a \code{\link[=plot_timeline]{plot_timeline()}}.
}
\details{
La tavola viene dedotta automaticamente dalla \code{variabile} quando
\code{tavola = NULL}. Ad esempio \code{variabile = "occupati_totale"} implica
\code{tavola == 2.2}, \code{variabile = "raggiunti"} implica \code{tavola == 2.1},
\code{variabile = "individui"} o \code{"totale"} implicano \code{tavola == 1.1}.
}
\examples{
# Occupati totale, Emilia-Romagna
s1 <- gol_extract_series(variabile = "occupati_totale",
                         etichetta = "Emilia-Romagna")

# Confronto multi-regione
s2 <- gol_extract_series(variabile = "raggiunti",
                         etichetta = c("Emilia-Romagna", "Lombardia",
                                       "Campania", "Sicilia"))

# Decomposizione per percorso (Emilia-Romagna)
s3 <- gol_extract_series(variabile = "presi_in_carico",
                         etichetta = "Emilia-Romagna",
                         percorso = NULL)

}
