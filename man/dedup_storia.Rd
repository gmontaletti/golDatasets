% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dedup_storia.R
\name{dedup_storia}
\alias{dedup_storia}
\title{Deduplica un dataset di storia lunga GOL}
\usage{
dedup_storia(data, keys)
}
\arguments{
\item{data}{Un \code{data.table} long con almeno le colonne \code{valore} e \code{fonte}.}

\item{keys}{Vettore character delle colonne che identificano la chiave
logica di un osservazione (es. \code{c("data_riferimento", "regione", "variabile", "percorso")}).}
}
\value{
Un \code{data.table} deduplicato con le stesse colonne, una riga per
chiave logica.
}
\description{
Applica regole di deduplicazione in ordine sulla chiave logica indicata:
\enumerate{
\item se piu' righe hanno \code{valore == 0} o \code{NA}, mantiene la prima riga
con valore non-zero/non-NA.
\item se i valori sono identici tra fonti diverse, mantiene una sola
riga (la fonte di priorita' piu' alta).
\item se i valori differiscono, mantiene la riga della fonte di
priorita' piu' alta (inapp_focus > storico_INAPP > MLPS > ANPAL > ALTRO).
}
}
\examples{
dt <- data.table::data.table(
  data_riferimento = as.Date("2025-01-31"),
  regione = "Lombardia", variabile = "x", percorso = NA,
  valore = c(0, 100, 100),
  fonte  = c("storico_ANPAL", "storico_MLPS", "storico_INAPP")
)
dedup_storia(dt, keys = c("data_riferimento", "regione",
                            "variabile", "percorso"))
# Tiene la riga storico_INAPP (priorita' piu' alta tra le non-zero)

}
