% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gol_quality.R
\name{gol_storico_quality}
\alias{gol_storico_quality}
\title{Calcola le metriche di qualita' per \code{gol_storico_regionale}}
\usage{
gol_storico_quality(data = NULL)
}
\arguments{
\item{data}{Un \code{data.table} con la stessa struttura di
\code{gol_storico_regionale}. Se \code{NULL} (default), usa il dataset esposto.}
}
\value{
Un \code{data.table} con le colonne \verb{file, ente, data_riferimento, tema, caption_num, n_rows, n_anchor, n_col_index, n_header_variants, pct_na_valore, header_per_col, severity}. Ordinato per severita'
decrescente.
}
\description{
Per ogni \verb{(file, tema, caption_num)} produce 4 metriche aggregate e
assegna una severita' usando \code{\link[=gol_quality_classify]{gol_quality_classify()}}.
}
\examples{
q <- gol_storico_quality()
q[severity != "ok", .N, by = .(ente, severity)]

}
