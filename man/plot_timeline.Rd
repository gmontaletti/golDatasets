% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeline.R
\name{plot_timeline}
\alias{plot_timeline}
\title{Disegna una timeline con eventuali annotazioni di rotture di metodo}
\usage{
plot_timeline(
  data,
  x = "data",
  y = "valore",
  group = NULL,
  ruptures = NULL,
  title = NULL,
  subtitle = NULL,
  y_label = NULL,
  date_breaks = "6 months",
  smooth = FALSE
)
}
\arguments{
\item{data}{Un \code{data.frame} o \code{data.table} in formato long con almeno una
colonna data e una colonna numerica.}

\item{x}{Nome (character) della colonna data sull'asse orizzontale.
Default: \code{"data"}.}

\item{y}{Nome della colonna numerica da plottare. Default: \code{"valore"}.}

\item{group}{Nome opzionale della colonna di raggruppamento (mappata su
colore + tipologia di linea). Lascia \code{NULL} per una singola serie.}

\item{ruptures}{Data frame con le rotture da annotare. Deve contenere
colonne \code{data} (date) ed \code{evento} (character). Passa
\code{gol_method_ruptures} per le rotture standard GOL. \code{NULL} (default) per
nessuna annotazione.}

\item{title, subtitle}{Titolo e sottotitolo del plot.}

\item{y_label}{Etichetta dell'asse Y.}

\item{date_breaks}{Stringa passata a \code{ggplot2::scale_x_date} (es. \code{"6 months"},
\code{"1 year"}). Default: \code{"6 months"}.}

\item{smooth}{Se \code{TRUE} aggiunge un geom_smooth(method = "loess").}
}
\value{
Un oggetto \code{ggplot}.
}
\description{
Funzione generica per visualizzare una serie temporale long. Pensata per i
tre dataset del package ma applicabile a qualunque \code{data.frame} con una
colonna data e una colonna valore. Le rotture di metodo (per i dati GOL
nel 2025) si attivano passando un data frame al parametro \code{ruptures};
tipicamente \code{gol_method_ruptures}.
}
\examples{
\dontrun{
library(data.table)
serie <- gol_inapp_mensile[
  tavola == 2.2 & etichetta == "Emilia-Romagna" &
  variabile == "occupati_totale" & percorso == "",
  .(data = data_riferimento, valore)
]
plot_timeline(serie, ruptures = gol_method_ruptures,
              title = "Occupati GOL - Emilia-Romagna",
              y_label = "N. occupati")
}

}
