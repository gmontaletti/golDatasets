% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storico_decoder.R
\name{gol_decode_storico}
\alias{gol_decode_storico}
\title{Applica il decoder semantico a gol_storico_regionale}
\usage{
gol_decode_storico(data = NULL, min_confidenza = "low")
}
\arguments{
\item{data}{Un \code{data.table} con la stessa struttura di
\code{gol_storico_regionale}. Se \code{NULL} (default), usa il dataset esposto.}

\item{min_confidenza}{Tiene solo le righe con \verb{confidenza >=} il livello
indicato. Default \code{"low"} (tutte). Per analisi rigorose usare \code{"high"}.}
}
\value{
Un \code{data.table} con le colonne originali piu' quelle semantiche.
}
\description{
Aggiunge le colonne \code{variabile}, \code{caratteristica}, \code{modalita}, \code{percorso},
\code{unita}, \code{confidenza} derivate dal lookup \code{storico_decoder} su
\verb{(tema, caption_num, col_index, era)}. La colonna \code{era} viene calcolata
al volo da \code{data_riferimento} (cut a 2025-01-01).
}
\examples{
d <- gol_decode_storico()
d[tema == "A1" & confidenza == "high",
  .(data_riferimento, anchor, variabile, percorso, valore_num)]

}
